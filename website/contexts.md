---
description: Every Trigger Lib context side by side, with the exact interface and method names for before insert, after insert, before update, after update, before delete, after delete and after undelete.
---

# Contexts at a Glance {#method-names}

Every interface you can implement, by context.

<!--@include: @/_parts/generated/matrix-methods.md-->
